version: 1

workflow:
  id: capacity.l1.shortLived
  name: CapacityL1ShortLived
  description: Capacity harness L1. Immediate complete via builtin noop.

nodes:
  - name: flow.start
    type: start
    label: start
    next: flow.noop

  - name: flow.noop
    type: action
    label: noop
    action: statevia.action.builtin.execution.noop
    next: flow.end

  - name: flow.end
    type: end
    label: end
